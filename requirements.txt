# Production dependencies (installed by Render via build.sh).
# Versions pinned to the ones verified for this project on Python 3.14.
Django==6.1.1
dj-database-url==3.1.2
gunicorn==26.2.0
psycopg[binary]==3.3.5
whitenoise==6.12.0

